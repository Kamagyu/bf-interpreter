# Brainfuck Interpreter

A straightforward Brainfuck interpreter written in OCaml. Run the main file to execute the program located in `source.bf`. Following the rules of https://brainfuck.net/learn.

[!WARNING]
Programs requiring input with `,` may behave weirdly when run within `utop`, run directly wit `ocaml` instead.